//Codigo SQL do banco de dados



CREATE TABLE livro 
( 
 cod_livro INT PRIMARY KEY,  
 titulo_livro INT,  
 editora_livro INT,  
 isbn_livro INT,  
 preço_livro INT,  
 publicação_livro INT,  
); 

CREATE TABLE aluno 
( 
 cod_aluno INT PRIMARY KEY,  
 cpf_aluno INT,  
 contato_aluno INT,  
 datanasc_aluno INT,  
 sexo_aluno INT,  
 endereço_aluno INT,  
 nome_aluno0 INT,  
); 

CREATE TABLE professor 
( 
 sexo_professor INT,  
 datanasc_professor INT,  
 endereço_professor INT,  
 contato_professor INT,  
 email_professor INT,  
 cpf_professor INT,  
 nome_professor INT,  
 cod_professor INT PRIMARY KEY,  
 ativo_professor INT,  
); 

CREATE TABLE autor 
( 
 cod_autor INT PRIMARY KEY,  
 nome_autor INT,  
 nacionalidade_autor INT,  
 datanasc_autor INT,  
 email_autor INT,  
); 

CREATE TABLE funcionario 
( 
 cod_funcionario INT PRIMARY KEY,  
 nome_funcionario INT,  
 cpf_funcionario INT,  
 ativo_funcionario INT,  
 contato_funcionario INT,  
 email_funcionario INT,  
); 

CREATE TABLE editora 
( 
 cod_editora INT PRIMARY KEY,  
 nome_editora INT,  
 contato_editora INT,  
 cnpj_editora INT,  
); 

CREATE TABLE pedido 
( 
 nome_pedido INT,  
 cod_pedido INT PRIMARY KEY,  
); 

CREATE TABLE emprestimo 
( 
 cod_emprestimo INT PRIMARY KEY,  
 data_emprestimo INT,  
 data_devolução INT,  
); 

CREATE TABLE escreve 
( 
 cod_livro INT PRIMARY KEY,  
 cod_autor INT PRIMARY KEY,  
); 

CREATE TABLE publica 
( 
 cod_livro INT PRIMARY KEY,  
 cod_editora INT PRIMARY KEY,  
); 

CREATE TABLE Rel 
( 
 cod_aluno INT PRIMARY KEY,  
 cod_emprestimo INT PRIMARY KEY,  
 cod_livro INT PRIMARY KEY,  
); 

CREATE TABLE registra 
( 
 cod_funcionario INT PRIMARY KEY,  
 cod_pedido INT PRIMARY KEY,  
); 

CREATE TABLE Rel1 
( 
 cod_professor INT PRIMARY KEY,  
 cod_emprestimo INT PRIMARY KEY,  
); 

ALTER TABLE escreve ADD FOREIGN KEY(cod_livro) REFERENCES livro (cod_livro)
ALTER TABLE escreve ADD FOREIGN KEY(cod_autor) REFERENCES autor (cod_autor)
ALTER TABLE publica ADD FOREIGN KEY(cod_livro) REFERENCES livro (cod_livro)
ALTER TABLE publica ADD FOREIGN KEY(cod_editora) REFERENCES editora (cod_editora)
ALTER TABLE Rel ADD FOREIGN KEY(cod_aluno) REFERENCES aluno (cod_aluno)
ALTER TABLE Rel ADD FOREIGN KEY(cod_emprestimo) REFERENCES emprestimo (cod_emprestimo)
ALTER TABLE Rel ADD FOREIGN KEY(cod_livro) REFERENCES livro (cod_livro)
ALTER TABLE registra ADD FOREIGN KEY(cod_funcionario) REFERENCES funcionario (cod_funcionario)
ALTER TABLE registra ADD FOREIGN KEY(cod_pedido) REFERENCES pedido (cod_pedido)
ALTER TABLE Rel1 ADD FOREIGN KEY(cod_professor) REFERENCES professor (cod_professor)
ALTER TABLE Rel1 ADD FOREIGN KEY(cod_emprestimo) REFERENCES emprestimo (cod_emprestimo)
